import ACTIONS from '../actions';
import DATA from '../da';

export default function rootReducer(action, state) {
  switch (action.type) {
    case ACTIONS.LOAD_DATA:
      state = 
    break;
  }

  return state;
}
